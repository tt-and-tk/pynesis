// 実行ファイル用: 文字列リテラルで初期化したグローバル配列は，メモリの後半の上端から宣言順に下へ置かれる
// (msgは0xFFFC番地，msg2は0xFFF8番地)．初期化とprintの処理自体はROM用と同じ
char msg[] = "AB";
char msg2[] = "C" "D";

int main(void) {
    print(msg);
    print(msg2);
}
