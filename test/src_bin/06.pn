// 実行ファイル用: グローバル変数とスタックの合計がメモリの半分(32768バイト)を超えてもエラーにならない
// (グローバル変数はメモリの後半，スタックは前半にあり，別の領域を使うため)
int table[8000];                  // 32000バイト
void f(void) {
    int buf[1000];                // 4000バイト
}
void main(void) {
    f();
}
