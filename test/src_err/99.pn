// expect: compiler error: print does not support an array member whose address is determined at run time at src_err/99.pn:10
// 構造体配列要素のメンバ配列をprintに渡すことは非対応 (実行時アドレス計算になるため)
struct Entry {
    char name[4];
};

struct Entry entries[2];

void main(void) {
    print(entries[0].name);
}
