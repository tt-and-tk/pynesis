// expect: compiler error: struct cannot be used as a value; access a member or take its address at src_err/295.pn:8
// 意味解析エラー: 構造体そのものはキャストできない(値として読めないため) (異常系)
struct Point { int x; int y; };
struct Point pt;
int r;

void main(void) {
    r = (int)pt;
}
