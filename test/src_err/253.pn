// expect: compiler error: struct cannot be used as a function parameter type at src_err/253.pn:7
// 構文解析エラー: 構造体そのものを受け取る関数へのポインタは宣言できない(構造体を関数の引数にできないため) (異常系)
struct Tag {
    int x;
};
void main(void) {
    void (*f)(struct Tag);
}
