// expect: compiler error: cannot take the address of 'N' (it has no memory address) at src_err/216.pn:5
// 意味解析エラー: const変数はメモリ上の番地を持たないため，番地を取れない (異常系)
const int N = 1;
void main(void) {
    int *p = &N;
}
