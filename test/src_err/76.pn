// expect: compiler error: cannot use void value in expression at src_err/76.pn:9
// 意味解析エラー: 関数呼び出しの引数にvoid値(scanの戻り値)は使えない (異常系)
char buf[8];
int add(int a, int b) {
    return a + b;
}
void main(void) {
    int x;
    x = add(1, scan(buf));
}
