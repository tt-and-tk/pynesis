// 字句解析エラー: 文字リテラルに2文字入っている (異常系)
// 01.cと同じthrow箇所(unterminated char literal)に落ちる．
// 字句解析器は文字本体を常に1文字(またはエスケープ1つ)だけ消費して閉じ'を期待するため，
// 'a'を消費した後の次の文字が'ではなく'b'であることが「未終端」として検出される
void main(void) {
    char x = 'ab';
}
