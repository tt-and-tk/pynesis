// expect: compiler error: expression is not a variable, array element, struct member or dereference at src_err/254.pn:7
// 意味解析エラー: 関数呼び出しの結果は番地を持たないため，++/--の対象にできない (異常系)
int zero(void) {
    return 0;
}
void main(void) {
    zero()++;
}
