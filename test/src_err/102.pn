// 構造体配列をメンバに持つこと(構造体のメンバとしての構造体配列)も，単一のネスト構造体メンバと
// 同じ理由(ネスト構造体は非対応)で非対応．メンバの型を読んだ時点でBASE_STRUCTを弾くため，
// 配列かどうか([要素数])を見るより前にエラーになる
struct Inner {
    int v;
};
struct Outer {
    struct Inner arr[3];
};

void main(void) {
}
