// expect: compiler error: invalid operands to binary '+' ('int (*)(void)' and 'int') at src_err/230.pn:8
// 意味解析エラー: 関数ポインタに整数は足せない(関数の番地をずらす意味がない) (異常系)
int zero(void) {
    return 0;
}
void main(void) {
    int (*f)(void) = zero;
    f = f + 1;
}
