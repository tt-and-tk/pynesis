// expect: compiler error: struct cannot be used as a value; access a member or take its address at src_err/273.pn:7
// 意味解析エラー: 構造体ポインタの指す先の構造体そのものは式文にできない (異常系)
struct Point { int x; int y; };
struct Point s;
void main(void) {
    struct Point *sp = &s;
    *sp;
}
