// expect: compiler error: cannot use void value in expression at src_err/73.pn:6
// 意味解析エラー: 三項演算子の条件式にvoid値(scanの戻り値)は使えない (異常系)
char buf[8];
void main(void) {
    int x;
    x = scan(buf) ? 1 : 2;
}
