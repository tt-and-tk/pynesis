// expect: compiler error: cannot apply '&' to function 'zero'; write the function name alone to get its address at src_err/268.pn:7
// 意味解析エラー: 関数の番地は関数名だけで書き，&を付けることはできない (異常系)
int zero(void) {
    return 0;
}
void main(void) {
    int (*f)(void) = &zero;
}
