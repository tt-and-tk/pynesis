// expect: compiler error: invalid operands to binary '+' ('int *' and 'int *') at src_err/232.pn:7
// 意味解析エラー: ポインタどうしは足せない(引き算で要素数の差を求めることのみできる) (異常系)
int arr[4];
int *q;
void main(void) {
    int *p = arr;
    q = p + p;
}
