// ポインタの指す先への複合代入と，*p++(指す先を読んでからポインタを進める)
// 指す先がcharなら，読み出しは符号拡張し，書き戻しは下位8ビットだけを書き込む
char cs[2];
int r;

void main(void) {
    char *p = cs;
    *p += 3;
    p[1] |= 4;
    r = *p++;         // cs[0]を読み，pはcs[1]を指す
}
