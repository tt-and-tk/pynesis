// #pragma onceを書いたファイルは，同じファイルから重複して取り込んでも，別のファイルを経由して間接的に重複して取り込んでも1度だけ取り込まれる
#include "include/49_common.pn"
#include "include/49_common.pn"
#include "include/49_user.pn"
void main(void) {
    count = count + step();     // 1 + 1 = 2
}
