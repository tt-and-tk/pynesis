// 取り込んだファイルで定義した定数・グローバル変数・関数を，取り込み元から使える
#include "include/51_lib.pn"    // 取り込み指令の後ろの同じ行に行コメントを書ける
#include "include/51_comment.pn"    /* 取り込み指令の後ろの同じ行にブロックコメントを書ける */
int g;
void main(void) {
    g = add_base(LIB_BASE);     // 2 + 3 = 5
}
