// 標準入力からの1行読み込み(scan)が，バックスペース(DEL, 0x7F)による1文字取り消しに対応していることを確認する
void main(void) {
    char x[4];      // ローカル配列 (scanの格納先)
    scan(x);        // 標準入力(1行) → ローカル配列x
    print(x);       // ローカル配列を出力
}
